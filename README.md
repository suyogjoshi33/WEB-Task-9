# Task 9 - Client Side Form Validation

This project demonstrates client-side form validation using JavaScript.
It validates name, email, password, and confirm password fields.
Error messages are displayed dynamically and form submission is prevented
until all validations pass.
